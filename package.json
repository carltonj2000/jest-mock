{
  "name": "jest-mock",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.4.2",
    "react-dom": "^16.4.2",
    "react-scripts": "1.1.4",
    "rxjs": "^6.2.2"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "test-bk": "node --inspect-brk ./node_modules/.bin/jest --env=node",
    "eject": "react-scripts eject"
  },
  "devDependencies": {
    "supertest": "^3.1.0"
  }
}
